{
  # nvim-autopairs — auto-insert the closing ) ] } " ' etc. when you type the
  # opening one, and handle newlines between pairs sensibly (like Zed / VS Code).
  plugins.nvim-autopairs.enable = true;
}
