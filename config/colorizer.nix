{
  # nvim-colorizer — renders color codes inline with their actual color
  # (#rrggbb, #rgb, rgb()/rgba(), hsl(), and named colors) across filetypes.
  # Handy for CSS/Tailwind/theme files. Defaults colorize every filetype.
  plugins.colorizer.enable = true;
}
