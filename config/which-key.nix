{
  # which-key — press a prefix (e.g. <Space>) and a popup lists the follow-up
  # keys with their descriptions. It reads the `desc` from every keymap we've
  # defined (telescope, LSP, gitsigns, trouble, …), so it doubles as a cheatsheet.
  plugins.which-key.enable = true;
}
